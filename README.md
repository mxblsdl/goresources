# System Resource Monitor

This project is a simple system resource monitor that tracks CPU and memory usage over time. It provides a command-line interface to display real-time statistics and logs the data for later analysis.